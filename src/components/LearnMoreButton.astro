---

---

<a href="#learn" class="download">
    <span class="download-button learn">Learn More</span>
</a>

<style>
    a {
        font-size: 20px;
        text-transform: uppercase;
    }
    .download {
        font-weight: bold;
        position: relative;
        z-index: 20;
        padding: 16px 8px;
    }

    .download-button.learn {
        background-color: rgba(235, 218, 156, 0.4);
    }
    .download-button {
        padding: 10px 30px;
        cursor: pointer;
        transition: 0.3s all ease;
    }
    .download:hover .download-button.learn {
        background-color: rgba(235, 218, 156, 0.6);
    }

    .download::before,
    .download::after {
        content: "";
        display: block;
        position: absolute;
        transition: 0.3s all ease;
        width: 80%;
        height: 50%;
        border: 1px solid transparent;
    }
    .download::before {
        border-left: 1px solid #fff;
        border-bottom: 1px solid #fff;
        left: 0;
        bottom: 0;
    }
    .download::after {
        border-right: 1px solid #fff;
        border-top: 1px solid #fff;
        top: 0;
        right: 0;
    }

    .download:hover::before,
    .download:hover::after {
        height: 75%;
        width: 50%;
    }

    .download:active::before {
        transform: translate(5%, -8%);
    }
    .download:active::after {
        transform: translate(-5%, 8%);
    }

    @media (max-width: 640px) {
        a {
            font-size: 16px;
        }
        .download-button {
            padding: 10px 18px;
        }
    }
</style>
